export default class Habitacion {
  constructor(id, nombre, precio, disponibles, descripcion, image) {
    this.id = id
    this.nombre = nombre
    this.precio = precio
    this.disponibles = disponibles
    this.descripcion = descripcion
    this.image = image
  }
}