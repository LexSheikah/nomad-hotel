export default class Huesped {
  constructor(id, nombre, apellido, email, telefono) {
    this.id = id
    this.nombre = nombre
    this.apellido = apellido
    this.email = email
    this.telefono = telefono
  }

  reservarHabitacion(idHabitacion, fechaInicio, fechaFin, cantidadPersonas) {
    // Codigo de la reservación
  }
}